{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "Backend for the phonebook app",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js",
    "build:ui": "rm -rf build && cd ~/FullStackOpenSubmissions/part2/phonebook && npm run build --prod && cp -r build ~/Projects/FSO/FSOPart3",
    "deploy": "git push heroku main",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push && npm run deploy",
    "logs:prod": "heroku logs --tail"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/denvicar/FSOPart3.git"
  },
  "author": "Ciro Faenza",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/denvicar/FSOPart3/issues"
  },
  "homepage": "https://github.com/denvicar/FSOPart3#readme",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongoose": "^6.0.12",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.14"
  }
}
